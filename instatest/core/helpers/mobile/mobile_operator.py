class MobileOperator:
    Equals = "="
    StartsWith = "StartsWith"
    EndsWith = ""
    Contains = "Contains"
    Matches = "Matches"   #  UiSelector().*Matches methods in uiautomator searches with a regex string  (textMatches, classNameMatches, etc)